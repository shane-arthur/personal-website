import React, { PropTypes, Component } from 'react';
import { CARD_STYLE } from '../constants/styles/Card-Styles';

export default class ProfileCard extends Component { 
    render() {
        return(
        <div style={CARD_STYLE}> My niggaz is savage, ruthless</div> 
        );
    }
}